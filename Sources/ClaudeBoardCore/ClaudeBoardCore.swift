/// ClaudeBoardCore — Domain logic, ports, adapters, and infrastructure for ClaudeBoard.
/// This library has no UI dependencies and is fully testable independently.
public enum ClaudeBoardCore {
    public static let version = "0.1.0"
}
